# 智能记账板

一个纯前端的智能记账网页，支持：

- 语音识别录入
- 自然语言识别金额与分类
- 日 / 周 / 月消费看板
- CSV / HTML / PDF 导出

## 本地使用

直接在浏览器中打开 `index.html` 即可使用。

## GitHub Pages 发布

仓库已包含 GitHub Pages 自动部署工作流。将代码推送到 GitHub 仓库的 `main` 分支后，可在仓库设置中启用 Pages，并选择 `GitHub Actions` 作为部署来源。
